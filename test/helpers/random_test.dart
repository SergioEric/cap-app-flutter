import 'package:cap_sahagun/helpers/helpers.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:lol_colors_flutter/lol_colors_flutter.dart';

void main() {
  test('random ...', () {
    // Testing random picked color
    int _random = random(all_lol_colors.length);

    // expect(_random, 2);
  });
}
